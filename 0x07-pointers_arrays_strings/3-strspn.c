#include <stdio.h>
/**
 *_strspn - capital each letter
 *@s: parameter
 *@accept: parameter
 *Return: return string
 */
unsigned int _strspn(char *s, char *accept)
{
char f = strspn(s, accept);
return (f);
}
