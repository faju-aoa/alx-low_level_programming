#include <stdio.h>
#include <string.h>
/**
 *_strcat - joining two string
 *@dest: parameter
 *@src: parameter
 *Return: return string
 */
char *_strncat(char *dest, char *src, int n)
{
strncat(dest, src, n);
return (dest);
}
