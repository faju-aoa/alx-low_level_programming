#include <stdio.h>
#include <string.h>
/**
 *_strcmp - function that compares s1 and s2
 *@s1:s1 pointer parameter;
 *@s2:s2 pointer parameter
 *Return: return void
 */
int _strcmp(char *s1, char *s2)
{
int p;
p = strcmp(s1, s2);
return (p);
}
