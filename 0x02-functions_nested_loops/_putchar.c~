#include <stdio.h>

/**
 * main - Entry point
 *
 *Retun: Always 0 (success)
 */
int main()
{
work();
return (0);
}
void work()
{
int i =0;
char letter[] = "_putchar\n";
while (letter[i])
{
char word = letter[i];
putchar(word);
i++;
}
}
