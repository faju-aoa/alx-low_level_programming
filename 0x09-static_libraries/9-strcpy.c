#include <stdio.h>
#include <string.h>
/**
 *_strcpy - string copy
 *@dest: parameter
 *@n: parameter
 *Return: return value
 */
char *_strcpy(char *dest, char *src)
{
char *p = strcpy(dest, src);
return (p);
}
