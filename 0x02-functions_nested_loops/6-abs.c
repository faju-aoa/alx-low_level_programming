#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 *_abs - check for absolute of number
 *@n:parameter for the function
 *Return: Return 1 and 0 if n is +, 0 or -
 */
int _abs(int n)
{
int num;
num = abs(n);
return (num);
}
