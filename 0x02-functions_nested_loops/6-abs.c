#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 *print_sign - check for sign of number
 *@n:parameter for the function
 *Return: Return 1 and 0 if n is +, 0 or -
 */ 
int _abs(int n)
{
int num;
num = abs(n);
_putchar('\n');
return (num);
}
